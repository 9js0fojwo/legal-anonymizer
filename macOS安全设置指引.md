# macOS 安全提示处理指引

## 遇到的问题

双击「**【请双击我！】启动脱敏工具.command**」时，macOS 弹出以下提示：

> **未打开"【请双击我！】启动脱敏工具.command"**
>
> Apple 无法验证"【请双击我！】启动脱敏工具.command"是否包含可能危害 Mac 安全或泄漏隐私的恶意软件。

这是 macOS 的正常安全机制（Gatekeeper），对所有非 App Store 下载的脚本都会拦截。本工具完全在本地运行，不联网、不上传任何数据。

---

## 解决方法

### 方法一：通过系统设置放行（推荐）

1. **弹出安全提示时，点「完成」**（不要点「移到废纸篓」）

2. **打开系统设置**
   - 点击屏幕左上角  苹果菜单 → **系统设置**

3. **进入「隐私与安全性」**
   - 在左侧栏选择 **隐私与安全性**

4. **找到拦截提示并放行**
   - 向下滚动页面，在底部「安全性」区域会看到：
   > "【请双击我！】启动脱敏工具.command"已被阻止使用，因为来自身份不明的开发者。
   - 点击右侧的 **「仍要打开」**

5. **输入密码确认**
   - macOS 会要求输入电脑密码或使用 Touch ID 验证

6. **再次双击运行**
   - 回到文件夹，再次双击「【请双击我！】启动脱敏工具.command」
   - 此次弹窗中会出现 **「打开」** 按钮，点击即可
   - 之后再次运行就不会再弹提示了

---

### 方法二：右键打开

1. 在文件上 **按住 Control 键点击**（或双指点击触控板）
2. 在弹出菜单中选择 **「打开」**
3. 弹出安全提示后，点击 **「打开」** 按钮

> 注意：macOS Sequoia (15.0+) 中此方法可能仍然被拦截，此时请使用方法一或方法三。

---

### 方法三：终端命令解除限制（一劳永逸）

如果上述方法仍无法解决，可以通过「终端」移除文件的隔离标记：

1. **打开终端**
   - 按 `Command + 空格` 打开聚焦搜索，输入 **终端**，回车

2. **执行以下命令**（复制粘贴即可）：

   如果工具放在「下载」文件夹中：
   ```
   xattr -cr ~/Downloads/legal-anonymizer
   ```

   如果放在其他位置，把路径替换为实际文件夹路径：
   ```
   xattr -cr /你的实际路径/legal-anonymizer
   ```

   > `xattr -cr` 的作用是移除文件夹中所有文件的「来自互联网」隔离标记，仅此而已，不会修改文件内容。

3. **再次双击即可正常运行**

---

### 方法四：直接用终端启动（无需处理安全设置）

如果你不想修改任何系统设置，可以直接通过终端启动：

1. **打开终端**（同上）

2. **执行以下命令**：
   ```
   cd /你的实际路径/legal-anonymizer
   python3 web_app.py
   ```

3. **打开浏览器访问** http://127.0.0.1:8080

---

## 为什么 macOS 会拦截？

- macOS 对从互联网下载的文件会自动添加「隔离标记」（quarantine attribute）
- 没有 Apple 开发者签名的脚本会被 Gatekeeper 拦截
- 这是 Apple 的安全策略，不代表文件有问题
- 本工具是开源脚本，完全本地运行，所有数据处理在你的电脑上完成，不会上传任何内容

---

## 常见问题

**Q: 点了「移到废纸篓」怎么办？**
A: 打开废纸篓，右键点击该文件选择「放回原处」即可恢复。

**Q: 系统设置里找不到「仍要打开」的选项？**
A: 需要先尝试打开一次被拦截后才会出现。如果仍找不到，请使用方法三（终端命令）。

**Q: 每次打开都要确认吗？**
A: 不需要。第一次放行后，之后双击都会直接运行。
